from simulation.models.inputs import DebtLoad, SettingsInput
from simulation.models.state import SimulationState

def apply_debt(state: SimulationState, load: DebtLoad, setting: SettingsInput):
    return state
