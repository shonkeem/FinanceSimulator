from simulation.models.inputs import ExpenseLoad, SettingsInput
from simulation.models.state import SimulationState

def apply_expense(state: SimulationState, load: ExpenseLoad, setting: SettingsInput):
    return state
